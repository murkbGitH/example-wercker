box: ngyuki/vagrant-virtualbox
build:
  steps:
    - script:
        code: |-
          packer validate -var-file=centos-7/variables.json template.json
          packer build    -var-file=centos-7/variables.json template.json
